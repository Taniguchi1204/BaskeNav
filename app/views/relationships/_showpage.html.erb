<% if current_user.following?(user) %>
　<a id="modan_open">Chatsを始める</a>
  <%= link_to "フォローを外す", user_relationships_path(user.id), method: :delete, class:"btn btn-success", remote: true %>
<% else %>
  <%= link_to "フォローする", user_relationships_path(user.id), method: :post, class:"btn btn-success", remote: true %>
<% end %>
