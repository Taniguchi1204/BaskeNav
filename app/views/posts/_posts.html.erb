<% posts.each do |post| %>
  <div>
    <%= link_to post_path(post) do %>
      <%= attachment_image_tag post.user, :user_image, :fill, 50, 50, fallback: "no_image.jpg" %>
      <%= post.user.name %>
      <%= post.content %>
    <% end %>
  </div>
<% end %>